name: Build and dockerize express.js app

on: [push]
jobs: 
  build:
    runs-on: ubuntu-latest
    step:
    - uses: actions/checkout@v3
    - name: Build the docker image
      run: docker build -t hello-devops:$(date +%s) .
